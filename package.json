{
  "name": "pult-zvonkov",
  "version": "1.0.0",
  "description": "Пульт контроля звонков",
  "scripts": {
    "build": "echo 'Static HTML project, no build needed'",
    "sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
